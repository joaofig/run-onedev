[project]
name = "run-onedev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "joaofig", email = "joao.figueira@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
run-onedev = "run_onedev:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
